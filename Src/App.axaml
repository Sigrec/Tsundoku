<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Tsundoku"
			 xmlns:vm="using:Tsundoku.ViewModels.MainWindowViewModel"
			 xmlns:cp="clr-namespace:Egorozh.ColorPicker.Dialog;assembly=Egorozh.ColorPicker.Avalonia.Dialog"
             xmlns:cv="clr-namespace:Tsundoku.Helpers"
             x:Class="Tsundoku.App">

    <Application.Styles>
        <FluentTheme Mode="Dark"/>
		<cp:FluentColorPickerTheme Mode="Dark"/>
		<!-- <StyleInclude Source="avares://Avalonia.Controls.ColorPicker/Themes/Fluent/Fluent.xaml" /> -->
    </Application.Styles>

	<Application.Resources>
		<cv:ColorConverter x:Key="ConvertColor"/>
	</Application.Resources>

    <TrayIcon.Icons>
		<TrayIcons>
			<TrayIcon Icon="\Assets\Icons\Tsundoku-Logo.ico" ToolTipText="Tsundoku">
				<!-- <TrayIcon.Menu>
					<NativeMenu>
						<NativeMenuItem Header="Open User Settings" ToggleType="Radio" IsChecked="False" Command="{Binding ToggleCommand}" />
						<NativeMenuItem Header="Open Theme Settings" ToggleType="Radio" IsChecked="True" Command="{Binding OpenThemeSettingsWindow}" />
						<NativeMenuItem Header="Open Add New Series Window" ToggleType="CheckBox" IsChecked="True" Command="{Binding ToggleCommand}" />
						<NativeMenuItem Header="Exit" Command="{Binding SaveOnClose}" />
					</NativeMenu>
				</TrayIcon.Menu> -->
			</TrayIcon>
		</TrayIcons>
  	</TrayIcon.Icons>
</Application>
